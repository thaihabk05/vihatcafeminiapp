name: Deploy Mini App to Zalo

on:
  # Triggered from Builder Studio via repository_dispatch
  repository_dispatch:
    types: [studio-deploy]
  # Also runnable manually from GitHub UI
  workflow_dispatch:
    inputs:
      tenant:
        description: "Tenant slug"
        required: true
        default: "vihat-cafe"
      env:
        description: "Deploy environment"
        type: choice
        options:
          - DEVELOPMENT
          - TESTING
        default: DEVELOPMENT
      description:
        description: "Version description"
        required: false
        default: "Deploy from Studio"

jobs:
  deploy:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: vihat-coffee
    # Job-level env so every step (including zmp-cli's internal vite rebuild
    # in the deploy step) sees the same VITE_* values. Missing
    # VITE_API_BASE here is what made the Studio-built bundle blank out
    # with "TypeError: Load failed" — it fell back to localhost which the
    # phone obviously can't reach.
    env:
      ADMIN_TOKEN: ${{ secrets.ADMIN_TOKEN }}
      BUILDER_API_BASE: ${{ secrets.BUILDER_API_BASE }}
      VITE_API_BASE: ${{ secrets.BUILDER_API_BASE }}
      VITE_DEFAULT_APP_ID: ${{ github.event.client_payload.tenant || inputs.tenant }}
      DEPLOY_ENV: ${{ github.event.client_payload.env || inputs.env }}
      DEPLOY_DESC: ${{ github.event.client_payload.description || inputs.description }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: vihat-coffee/package-lock.json

      - name: Install deps
        run: npm ci --no-audit --no-fund

      - name: Install zmp-cli (CI-only, not in package.json)
        run: npm install --no-save --no-audit --no-fund --legacy-peer-deps zmp-cli@^4.0.3

      - name: Configure zmp-cli auth
        run: |
          mkdir -p $HOME/.zmp
          cat > $HOME/.zmp/config.json <<EOF
          {
            "accessToken": "${{ secrets.ZMP_TOKEN }}",
            "appId": "1352472476106621006"
          }
          EOF
          echo "ZMP_TOKEN=${{ secrets.ZMP_TOKEN }}" >> $GITHUB_ENV
          echo "APP_ID=1352472476106621006" >> $GITHUB_ENV

      - name: Build production bundle (sanity check)
        run: npm run build

      - name: Deploy to Zalo + auto-save URL
        # `-p` runs non-interactive; `-t` picks Testing tier (else Zalo
        # treats versionStatus undefined → Development tier). zmp-cli's
        # OWN internal vite rebuild kicks in here — that's why we need
        # VITE_API_BASE at the JOB env level, not just on the build step.
        run: |
          FLAGS="-p"
          if [ "$DEPLOY_ENV" = "TESTING" ]; then FLAGS="$FLAGS -t"; fi
          node scripts/zmp-deploy.js $FLAGS -m "$DEPLOY_DESC"

      - name: Summary
        if: always()
        run: |
          echo "::notice::Deploy finished tenant=${VITE_DEFAULT_APP_ID} env=${DEPLOY_ENV}"
